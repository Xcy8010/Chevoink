# 创作执行可靠性审计 · 2026-09-13

## 范围与统计

只读检查生产记录，窗口为 2026-09-11 09:34:34 UTC 至 2026-09-13 09:34:34 UTC（香港时间各加 8 小时）。不修改用户正文、记忆、任务状态或模型配置。本文仅记录聚合结果，不含用户身份、正文、密钥或日志原文。

- 窗口内更新的任务 299 个：完成 194、失败 81、暂停 22、运行中 2。此为任务快照，不是同一开始时间队列的成功率。
- 窗口内 `tool.result` 5,693 条，其中 `ok=false` 204 条（约 3.58%）；另有运行级 `error` 事件 74 条。运行终态不一定产生 error 事件，两者不可相加当作失败任务数。
- `ok=false` 包含来源保护、质量前置条件、主动中断和真实执行故障，不能全部视为程序错误。

## 发现与处置

| 类别 | 观测 | 本轮处置 / 边界 |
| --- | --- | --- |
| 空思考与无交付 | 用户截图匹配任务共消耗 288,660 Token；5 轮各输出 8,192 Token，仅思考，无正文、无工具 | 独立识别空响应；最多一次纠正，第二次仍无交付则准确报错。无效思考保留诊断但不继续堆入下一轮请求；不改模型、推理档位或预算 |
| 工具参数解析 | 51 次，含场景任务 23、计划保存 10、正文写入 8 等 | 日志证明部分为供应商 `length` 截断，另一些为非 length 的不完整语法。截断单独标识；仍禁止执行补括号后的残缺写入；保留三次参数失败保护。未取得原始损坏参数的场景，不宣称全部语法原因已定位 |
| 记忆失败 | `memory_save` 执行失败 30 次，日志含来源版本、引用不符、作者删除 | 给出明确分类和读取原文/版本的恢复步骤；同参数同错误反复失败有界停止；不得去掉来源或改名绕过删除 |
| 删除记忆阻塞提交 | 章节桥提交两次因 `MEMORY_DELETED` 失败 | 可选记忆候选尊重删除并跳过，不回滚已验证的章节桥；其他错误仍回滚。提交产生的模型摘要/终态记忆保持待审候选，不自动当作者确认 |
| 推理回传 | 8 次供应商要求回传 `reasoning_content` | 发现压缩会产生没有原推理的合成 assistant 回合，改为明确标注的历史数据；近期原生工具回合与推理原样保留。不伪造思考，不保证覆盖所有第三方网关差异 |
| 连接中断 | 7 次 `terminated`，另有 502/504、模型服务异常 | 连接断开转为可识别模型流中断错误，保留已知用量，不执行部分工具；质量工具沿用有界重试，不把服务失败当质量不合格 |
| 目标与阶段 | 章节 ID 失效 13、质量目标不匹配 12，以及编译缺失/阶段冲突 | 保留精确归属校验，增加目录、卷列表、编译阶段与版本恢复引导，不猜测目标或重建绕过检查 |
| 质量前置 | 等待质量检查 12、修订后需重验连续性 11、证据定位失败 8 等 | 保留保护。新版正文必须重验；报告格式/引用不可靠时不可宣称通过 |
| 额度及配置 | 余额不足 16、用量缺失保护 21、每日额度用尽 2、模型名称不支持 1 | 需要供应商/账号配置或额度处理，不能靠自动重试解决；不代改模型、充值或关闭计费保护 |
| 数据库超时 | 1 次全局额度设置初始化路径超过 5 秒事务限制 | 既有设置走实时只读查询，缺失才初始化，减少热路径工作；不缓存暂停配置，不放大事务超时或承诺消除数据库故障 |

## 验证与发布边界

新增回归覆盖空响应恢复/停止、工具原生推理回传、源错误重试上限、流中断、设置读取和记忆删除后的章节提交。生产数据仅用于只读调查；数据库回归必须在隔离测试库执行。CI、部署和线上验收结果分别记录，不以错误提示优化宣称真实故障已全部消失。
